{
  "release_tag": "v0.8.6",
  "requested_at": "2026-07-22T17:30:00Z",
  "reason": "Publish BatikCraft Studio 0.8.6: stage 2 step 5. The library can now be built from a full raster document instead of per-object. Save to Asset Library flattens the whole document to a single PNG and stores it in a chosen user library (which must be created first, as before), reusing the existing import pipeline."
}
