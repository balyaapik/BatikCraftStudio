{
  "release_tag": "v0.8.7",
  "requested_at": "2026-07-22T18:40:00Z",
  "reason": "Publish BatikCraft Studio 0.8.7: fixes exported PNGs that could not be opened (Windows cannot find the file). PNG export now encodes to memory, verifies the data is non-empty, then writes atomically with fsync and os.replace, so a partial or 0-byte file can never be left on disk. On failure no file is created at all."
}
